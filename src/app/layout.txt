import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { ThemeProviderWrapper } from "@/components/ThemeProviderWrapper";
import Logo from "@/components/Logo";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Nerion",
  description: "Your mirror AI. It knows you. It connects you.",
  icons: {
    icon: "/favicon.ico", // ← ça suffit, pas besoin de balise manuelle si tu utilises `icons`
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={`${geistSans.variable} ${geistMono.variable} font-sans`}>
        <ThemeProviderWrapper>
          <header className="p-4 bg-gray-100 dark:bg-neutral-900 flex items-center">
            <Logo className="h-12 w-auto text-black dark:text-white" />
          </header>
          {children}
        </ThemeProviderWrapper>
      </body>
    </html>
  );
}
