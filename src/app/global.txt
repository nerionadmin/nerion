@tailwind base;
@tailwind components;
@tailwind utilities;

/* =========
   THEME TOKENS
   - Default: DARK (garde exactement ton rendu actuel)
   - Light: activer via la classe .light sur <html> (ou next-themes)
   ========= */

:root {
  /* === DARK (défaut) === */
  /* Nouvelles variables sémantiques (Nerion – Matching Focus) */
  --bg: #0C0D0F;
  --surface: #17191C;       /* surface-1 */
  --surface-2: #22252B;     /* surface-2 (cards/sections) */
  --border: #2C2F36;

  --text: #ECEDEE;
  --text-muted: #A3A8B0;

  /* CTA neutre (style Grok) + accent réservé aux détails */
  --primary: #2A2F36;       /* CTA principal (graphite doux) */
  --primary-hover: #343A3F; /* Hover subtil mais visible */
  --accent: #7A88FF;        /* Accent (liens/toggles) */
  --accent-hover: #95A2FF;
  --ring: #4C51BF;

  --success: #34D399;
  --warning: #FBBF24;
  --danger: #F87171;

  --image-border: #2C2F36;

  /* === Alias rétro-compatibilité (tes variables existantes) === */
  --background: var(--bg);
  --foreground: var(--text);
  --card-bg: var(--surface-2);
  --placeholder: var(--text-muted);
}

/* === LIGHT THEME === */
.light {
  --bg: #F3F3F1;
  --surface: #FFFFFF;       /* surface-1 */
  --surface-2: #FFFFFF;     /* surface-2 (bars/blocks) */
  --border: #D9DEE3;

  --text: #1F2124;
  --text-muted: #686E76;

  /* CTA neutre (cohérent avec Grok) + accent miroir pour les détails */
  --primary: #1F242B;       /* CTA principal (graphite) */
  --primary-hover: #2A3038; /* Hover subtil mais visible */
  --accent: #6E7EFF;        /* Accent (liens/toggles) */
  --accent-hover: #8A97FF;
  --ring: #C7D2FE;

  --success: #D1FAE5;
  --warning: #FEF3C7;
  --danger: #FECACA;

  --image-border: #D9DEE3;

  /* Alias rétro-compat pour tes variables existantes */
  --background: var(--bg);
  --foreground: var(--text);
  --card-bg: var(--surface-2);
  --placeholder: var(--text-muted);
}

html, body {
  @apply m-0 p-0 h-full bg-[var(--background)] text-[var(--foreground)] font-sans;
}
